
/*Texto interativo
const textos = document.querySelectorAll('.texto');

textos.forEach(text => {
    text.innerHTML = text.textContent
        .split('')
        .map(char => `<span>${char}</span>`)
        .join('');
});

*/

